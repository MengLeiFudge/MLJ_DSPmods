# Blueprint Planet 蓝图星球

新建原版蓝图时，自动把当前星球名称写入蓝图属性，并作为缩略图文字默认值。

## 功能

- 自动新建原版扩展属性：`蓝图星球`。
- 属性值使用当前星球的显示名称，包含玩家改名。
- 缩略图文字默认使用同一星球名称，不再是“新的蓝图”。
- 两个默认值都能继续通过原版蓝图检查器编辑。

## 兼容性

- 不依赖 Fractionate Everything 或其他 Mod。
- 不修改蓝图编码格式；“蓝图星球”使用原版 `externalFields` 保存，未安装本 Mod 时原版仍会保留和显示该属性。
- 已保存蓝图不会被修改。
